#include <stdio.h>
#include <stdlib.h>



typedef struct dados_unidade{

    char nomeunidade[20];
    char localizacao[50];
    char horariofunc[20];
    char telefone[15];
    
}unidade; //apelido da unidade
